/**
 * The following assigns two variables of types string and number and prints
 * the variables' types to the console.
 */

let x: string = "string";
let y: number = 27;

console.log(`The type of x is ${typeof(x)} and y is ${typeof(y)}`);