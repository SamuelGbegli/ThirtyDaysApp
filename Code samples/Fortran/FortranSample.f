C The following program defines a character array called greeting, assigns the value "Hello, world"
C and prints the value

program fortranSample

    character(len = 12) :: greeting
    greeting = "Hello, world"
    print *, greeting

end program fortranSample