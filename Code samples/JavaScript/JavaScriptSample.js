/*
 * The following generates a browser prompt asking the user to enter their name,
 * then creates a browser alert with the inputted name.
 */

let input = prompt("What is your name?");
alert(`Hello ${input}`);