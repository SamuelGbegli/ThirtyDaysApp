/*
    The following defines two values, then uses interpolation to print a value
    and a sum with a string to the console.
*/

fn main() {
    let x = 2;
    let y = 3;
    println!("x = {x}, x + y = {}", x + y);
}
